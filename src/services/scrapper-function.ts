// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for ScrapperFunction
 */
export interface ScrapperFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/services/scrapper.
 */
export class ScrapperFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: ScrapperFunctionProps) {
    super(scope, id, {
      description: 'src/services/scrapper.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/services/scrapper.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}