// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for DetailsFetcherFunction
 */
export interface DetailsFetcherFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/services/details-fetcher.
 */
export class DetailsFetcherFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: DetailsFetcherFunctionProps) {
    super(scope, id, {
      description: 'src/services/details-fetcher.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/services/details-fetcher.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}